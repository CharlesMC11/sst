# Functional Specification

1. A **WATCHER** monitors a folder, then lists the new screenshots listed under it
2. A **VALIDATOR** goes through each file to validate that each file is an image
3. A **SORTER** uses natural sorting to sort filenames
4. A **PROCESSOR** injects metadata into the screenshots
5. An **ARCHIVER** archives the original files
